apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: logging-system
  labels:
    app: grafana
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30085
      name: http
  selector:
    app: grafana
