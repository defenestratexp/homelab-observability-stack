apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: logging-system
  labels:
    app: loki
spec:
  type: NodePort
  ports:
    - port: 3100
      targetPort: 3100
      nodePort: 31100
      name: http
    - port: 9096
      targetPort: 9096
      name: grpc
  selector:
    app: loki
